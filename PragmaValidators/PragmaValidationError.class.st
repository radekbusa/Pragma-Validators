"
I represent a validation error of a PragmaValidator class method.

I get raised if and only if a given validator finds a problem with a value being validated.

"
Class {
	#name : #PragmaValidationError,
	#superclass : #Error,
	#category : #'PragmaValidators-Errors'
}
